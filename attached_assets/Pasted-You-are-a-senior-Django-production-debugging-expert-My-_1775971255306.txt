You are a senior Django production debugging expert.

My Django project (Finance Tracker) email OTP:
- Works in Replit (local environment)
- Still does NOT work after deployment on Render
- I am already using Gmail App Password (correct)

So the issue is NOT password — find the real production bug and FIX it.

-------------------------------------

1. VERIFY ENV VARIABLES (CRITICAL)

- Check if EMAIL_HOST_USER and EMAIL_HOST_PASSWORD are actually loaded
- Add debug logs:

print("EMAIL_HOST_USER:", os.getenv("EMAIL_HOST_USER"))
print("EMAIL_HOST_PASSWORD exists:", bool(os.getenv("EMAIL_HOST_PASSWORD")))

- If None → explain that Render environment variables are missing

-------------------------------------

2. CHECK DJANGO SETTINGS

Ensure:

EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True

- Confirm no typo
- Confirm no duplicate conflicting settings

-------------------------------------

3. FIND REAL ERROR (IMPORTANT)

Modify email sending code:

- Set fail_silently=False
- Add full exception logging

try:
    send_mail(...)
except Exception as e:
    import traceback
    print("EMAIL ERROR:", str(e))
    traceback.print_exc()

-------------------------------------

4. CHECK RENDER-SPECIFIC ISSUES

- Ensure DEBUG = False does not hide logs
- Ensure ALLOWED_HOSTS is correct
- Ensure no blocking in production settings

-------------------------------------

5. TEST EMAIL MANUALLY

Create a test endpoint or command:

def test_email(request):
    send_mail(
        "Test Email",
        "This is a test",
        EMAIL_HOST_USER,
        ["your_email@gmail.com"],
        fail_silently=False
    )

- This helps confirm if SMTP works independently of OTP logic

-------------------------------------

6. COMMON PRODUCTION ROOT CAUSES

Check and fix:

- Environment variables not set in Render dashboard
- App password incorrectly copied (spaces issue)
- Firewall/network blocking SMTP (rare)
- Gmail daily sending limits
- Missing TLS/SSL config

-------------------------------------

7. FINAL OUTPUT

Provide:

1. Debugged email function
2. Fixed settings.py (if needed)
3. Logs showing actual error cause
4. Exact reason why it failed on Render
5. Step-by-step final fix

IMPORTANT:
- Do not assume — find actual error
- Focus on debugging first, then fixing
- Keep changes minimal and safe