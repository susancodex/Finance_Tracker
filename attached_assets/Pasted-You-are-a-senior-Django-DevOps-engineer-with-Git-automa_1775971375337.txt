You are a senior Django DevOps engineer with Git automation experience.

Project:
Finance Tracker (already working in Replit, deployed on Render)

Problem:
- Email OTP works in Replit
- Email OTP does NOT work in Render (production)
- Gmail App Password is already used

Your task is to:
1. DEBUG
2. FIX
3. COMMIT
4. PUSH to GitHub

----------------------------------------

STEP 1: DEBUG ROOT CAUSE

- Locate OTP email sending logic
- Check if send_mail() is actually triggered
- Add debug logs:

print("Sending OTP to:", email)
print("EMAIL_HOST_USER:", os.getenv("EMAIL_HOST_USER"))

- Detect:
  - Missing environment variables
  - SMTP errors
  - Silent failures

----------------------------------------

STEP 2: FIX EMAIL CONFIGURATION

Update settings.py:

import os

EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = os.getenv('EMAIL_HOST_USER')
EMAIL_HOST_PASSWORD = os.getenv('EMAIL_HOST_PASSWORD')

----------------------------------------

STEP 3: FIX OTP EMAIL FUNCTION

- Ensure fail_silently=False
- Add proper error logging

try:
    send_mail(
        subject,
        message,
        EMAIL_HOST_USER,
        [email],
        fail_silently=False
    )
    print("OTP email sent successfully")
except Exception as e:
    import traceback
    print("EMAIL ERROR:", str(e))
    traceback.print_exc()

----------------------------------------

STEP 4: ADD ENV VALIDATION

if not EMAIL_HOST_USER or not EMAIL_HOST_PASSWORD:
    print("ERROR: Email environment variables missing!")

----------------------------------------

STEP 5: CREATE .env.example

EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

----------------------------------------

STEP 6: PREPARE FOR RENDER

Add comments in settings.py:

# IMPORTANT:
# Set EMAIL_HOST_USER and EMAIL_HOST_PASSWORD in Render dashboard
# Do NOT use Gmail normal password, use App Password

----------------------------------------

STEP 7: GIT AUTOMATION

Run these commands:

git add .
git commit -m "Fix: Email OTP not working in Render (SMTP + env + logging fix)"
git push origin main

----------------------------------------

STEP 8: OUTPUT

Show:
- What was the issue
- What files were changed
- Git commit confirmation

IMPORTANT:
- Do NOT break existing features
- Keep code clean and minimal
- Focus on production reliability