I already have an existing Finance Tracker project from this GitHub repository:

https://github.com/susancodex/Finance_Tracker.git

Modify the existing project authentication system with the following requirements.

========================================
AUTHENTICATION CHANGES
========================================

Remove all email OTP verification functionality completely.

This includes:
- OTP generation
- OTP model/table/fields
- OTP sending through email
- OTP verification pages/forms
- verification middleware
- any verification status field
- any email verification logic in signup flow

After signup, the user should be able to log in immediately using only:

- email
- password

No OTP or verification step should exist.

========================================
LOGIN / SIGNUP SYSTEM
========================================

Keep only simple authentication:

Signup:
- full name
- email
- password
- confirm password

Login:
- email
- password

Use secure password hashing.

Add proper validation:
- email must be unique
- password minimum 8 characters
- strong password validation
- proper error messages

========================================
REMOVE FORGOT PASSWORD
========================================

Remove complete forgot password functionality.

Delete:
- forgot password page
- reset password route
- email reset link logic
- token generation
- token verification
- reset password templates
- reset password backend views/controllers

There should only be:
- signup
- login
- logout

========================================
RENDER DEPLOYMENT READY
========================================

Make the project fully ready for Render deployment.

Please update everything required for production deployment:

1. requirements.txt
Include all required dependencies.

2. Procfile
Add proper command for Render deployment.

Example:
web: gunicorn projectname.wsgi

3. settings.py
Update for production:
- ALLOWED_HOSTS
- STATIC_ROOT
- STATICFILES_DIRS
- DEBUG using environment variable
- SECRET_KEY using environment variable
- database config using DATABASE_URL
- whitenoise for static files

4. Add .env support
Use python-decouple or os.environ

Required variables:
- SECRET_KEY
- DEBUG
- DATABASE_URL

5. Add render.yaml if needed

6. Configure PostgreSQL support for Render

Use:
dj-database-url
psycopg2-binary

========================================
CODE CLEANUP
========================================

Remove unused files, routes, templates, views, forms, and models related to:
- OTP
- email verification
- forgot password

Refactor code cleanly.

Keep folder structure clean and production-ready.

========================================
UI REQUIREMENTS
========================================

Keep authentication UI modern and simple:
- clean login page
- clean signup page
- responsive design
- proper bootstrap or CSS styling
- good error messages

========================================
IMPORTANT
========================================

Do NOT create a new project.

Modify the existing GitHub project codebase only.

Keep everything else in the finance tracker working normally:
- dashboard
- income
- expenses
- reports
- charts
- user session

Only simplify the authentication flow.

Return the complete updated code.