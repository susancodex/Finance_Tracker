# Backend API URL — set to your deployed Render backend URL
# In development this is left empty; Vite proxy handles /api/* → localhost:8000
# In production on Render, set this to your backend service URL
VITE_API_URL=https://finance-tracker-backend.onrender.com
